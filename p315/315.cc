#include <algorithm>
#include <cstring>
#include <iostream>
#include <sstream>
#include <vector>


#define MAXN 99

using namespace std;

vector< int > graph[ MAXN ];




int main( ) {
	int n, k, adj;
	while( cin >> n && n > 0 ) {
		for( int i = 0 ; i < n ; i++ ) 
			graph[ i ].clear( );

		while( sin >> adj ) {
			//TODO
		}

	}
}